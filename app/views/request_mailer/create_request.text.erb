Se ha creado la siguiente solicitud de vacaciones:

Detalle de Solicitud

  Solicitante: <%= get_user_with_email(@request.requester_email) %>
  Jefe Inmediato: <%= get_user_with_email(@request.approver_email) %>
  Unidad de Negocio: <%= @vacations ? @vacations.company : '' %>
  Rango de Vacaciones <%= @request.begin_at.strftime("%Y/%m/%d") %> a <%= @request.end_at.strftime("%Y/%m/%d") %>
  Fecha de regreso a la oficina: <%= @request.return_at %> 
  Días Disponibles: <%= pluralize(@vacations ? @vacations.available_days : '0', 'día', 'días') %> a <%= @last_month.strftime("%Y/%m/%d") %>
  Días Solicitados: <%= pluralize(@request.days, 'día', 'días') %>
  Días a Pagar: <%= pluralize(@request.pay_days, 'día','días') %>
  Adjuntó Carta: <%= @request.letter_file ? 'Si' : 'No' %>
  Observaciones: <%= @request.comments %>
  Fecha de Solicitud: <%= @request.requested_at.in_time_zone('America/Bogota').strftime("%Y/%m/%d - %I:%M%p") %>
  Estado: <%= @request.status %>